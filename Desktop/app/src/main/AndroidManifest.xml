<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mygameapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MadLibsDisplay"></activity>
        <activity
            android:name=".MadScroll"
            android:label="@string/title_activity_mad_scroll"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Game" />
        <activity android:name=".MadLibsActivity" />
        <activity android:name=".LotteryActivity" />
        <activity android:name=".LotteryDisplay" />
        <activity android:name=".FortuneDisplay" />
        <activity android:name=".FortuneActivity" />
        <activity android:name=".PoemDisplay" />
        <activity android:name=".PoemActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>